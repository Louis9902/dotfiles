#!/bin/bash
# Configuration file for bash (~/.bashrc)

# This file will be read for interactive shells unless the '--norc'
# flag is used to launch the shell.
# This is only done when it's not an login shell.

main() {
	echo >/dev/null
}


[[ $- == *i* ]] && main "$@"
unset -f main